public enum ESwitcherStatus {
    ON,
    OFF
}
public enum EElectricItem {
    FCO,
    DS,
}
public enum EDialogType {
    DialogScroll,
    DialogConfirm,
    DialogNotice,
}
public enum LogLevel {
    Debug,
    Warn,
    Error
}